/**
 * Phones already stop a video when the app goes to the background, so there is
 * nothing to do here. The browser twin of this file is where the work is.
 */
export function usePauseWhenHidden() {}
